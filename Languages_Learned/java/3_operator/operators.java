public class operators {

    static void main(String[] args) {
        // arithmetic operator
        int a = 12;
        int b = 3;

        int c = a + b;
        int d = a - b;
        int e = a * b;
        int f = a / b; // it gives quotient
        int g = a % b; // it gives remainder

        System.out.println(c);
        System.out.println(d);
        System.out.println(e);
        System.out.println(f);
        System.out.println(g);

        /*
        //  Assingment operator
         * operator----Example----------Equivalent
         * =  ------------- a = b ;-------     a=b;
         * += ------------- a +=b ;---------   a=a+b ; 
         * -= ------------- a-= b ;---------   a=a-b;
         * =  ------------- a*=b  ;----------- a=a*b;
         * /= ------------- a /=b ;----------  a=a/b;
         * %= ------------- a%=b  ;----------- a=a%b;
         * 
         */

        /*
            // relational operator
            ==  is Equal to 
            !=  not Equal to 
            >  Greater than
            <  Smaller than 
            >= Greater than with equal to 
            <= smaller than with equal to

        */

        /*
            logical operator
            && AND
            || OR
            !  NOT

        */

        /*
            Bitwise operator
            ~   Biwise Complement
            <<  Left shift
            >>  Right shift
            >>> Unsigned right shift
            & Bitwise AND
            ^ Bitwise exclusive OR

        */

        /*
            increment    ++
            decrement    --

        */

        /*
            ternary operator-----
        */

        /*
            Various Input Types using Scanner
            --we can use nextLong(),nextFloat(),nextDouble(),and next() methods to get long, float, double, and string input respectively from the user.
            -It is recommended to close the scanner object once the input is taken using the close() method
        */
    }

}
